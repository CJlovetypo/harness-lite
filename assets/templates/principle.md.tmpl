<!-- managed-by: harness-lite v1 -->
# {{PROJECT_NAME}} 项目原则

## 文档定位

本文件保存跨迭代、长期有效且经用户明确批准的产品/项目原则。原则约束 PRD 与 SPEC；不得为了配合某次实现而无痕改写。

main 上本文件的 committed bytes 与 SHA-256 是所有 Local/worktree/candidate/integration 的唯一当前原则身份。worktree 中的差异只是 proposal。

## 已批准原则

尚无已批准的项目专属原则。初始化 Harness 不等于授权 Agent 代替用户发明长期取舍。

新增原则时使用稳定 ID，例如：

```markdown
### P-001：<原则标题>

<原则正文、执行解释与冲突取舍>
```

## 冲突处理

- 原则与 PRD 冲突时，暂停冲突范围并请求用户决定。
- PRD 与 SPEC 冲突时，以已批准 PRD 为基线，回到需求或规格批准门。
- 实现前已知的变化必须修订并重新批准受影响的 PRD/SPEC；实现后的 deviation 只记录事实差异，不构成例外权威。

## 变更控制

- 只有用户明确批准后才能新增、修改、废止原则或调整优先级。
- 每次变化绑定 stable change ID、精确 before/after 文本、global principle lease、批准证据和所有开放 PRD 的影响审计。
- 保留原批准语义，通过变更记录说明变化；不要无痕重写历史基线，也不要自动 union 或按合并顺序裁决。

## 变更记录

| 日期 | 变更 | 批准依据 |
|---|---|---|
| {{DATE}} | 初始化原则文档；尚未批准项目专属原则 | 用户授权初始化 Harness |
